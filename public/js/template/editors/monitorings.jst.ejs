<div class="scrollable">
  <table class="table table-bordered index-table table-condensed">
    <thead>
      <tr>
        <th class="id">ID</th>
        <th class="delete">Delete</th>
      </tr>
    </thead>
    <tbody>
      <%
        var monitorings = get('monitorings');
        for(var i = 0; i < monitorings.length; i++) {
          var monitoring = monitorings[i];
      %>
      <tr>
        <td class="reference">
          <select name="reference">
            <option></option>
            <% for(var j = 0; j < this.monitorings.length; j++) { %>
            <option value="<%- this.monitorings[j].id %>"><%- this.monitorings[j].name %></option>
            <% } %>
          </select>
        </td>
        <td class="delete"><span class="button glyphicon glyphicon-remove"></span></td>
      </tr>
      <% } %>

      <% if(!this.options.readonly) { %>
      <%= this.partial('editors/_monitorings_new') %>
      <% } %>
    </tbody>
  </table>
</div>

